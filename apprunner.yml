version: 1.0 
runtime: nodejs12 
build:  
  commands:    
    pre-build:      
      - npm install --save      
      - npm i nodemon --save    
    # build:      
    #   - npm start
    # post-build:      - node node_modules/ejs/postinstall.js  
    # env:    - name: MY_VAR_EXAMPLE      
                # value: "example" 
run:  
  runtime-version: 12.21.0  
  command: npm start
  network:    
    port: 8000    
    # env: APP_PORT  
    # env:    
    #- name: MY_VAR_EXAMPLE      
    #value: "example"